namespace Klarna.Rest.Core.Model.Enum
{
    public enum OrderManagementOrderStatus
    {
        AUTHORIZED,
        PART_CAPTURED,
        CAPTURED,
        CANCELLED,
        EXPIRED,
        CLOSED
    }
}